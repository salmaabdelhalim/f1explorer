// export const usePagination = ({ itemsPerPage = 10 }) => {
//   const [page, setPage] = useState(1);
//   const paginatedItems = seasons.slice(
//     (page - 1) * itemsPerPage,
//     page * itemsPerPage
//   );

//   const totalPages = Math.ceil(seasons.length / itemsPerPage);
//   return { totalPages: totalPages, paginatedItems: paginatedItems };
// };
